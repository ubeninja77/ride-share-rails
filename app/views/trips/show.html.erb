<h2>Trip details</h2>
<p>Trip date: <%= @trip.date %></p>
<p>Driver: <%= link_to @trip.driver.name, driver_path %></p>
<p>Passenger: <%= link_to @trip.passenger.name, passenger_path%></p>

<p>Trip cost: <%= number_to_currency(@trip.cost.to_f/100) %></p>
<p>Trip rating: <%= @trip.rating %></p>
<p><%= link_to "Complete Trip", edit_trip_path(@trip) %></p>
<p><%= link_to "Delete Trip", trip_path(@trip), method:  :delete, data: {confirm: "Are you sure you want to delete this trip?"} %></p>
<p><%= link_to "#{@trip.passenger.name}'s Trips List", passenger_trips_path(@trip.passenger_id)%></p>
